import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'new_web_server',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
